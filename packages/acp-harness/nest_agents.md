# Your workspace

This directory is shared by every agent on this machine. It is where you keep
what you learn, and where the repositories you work in live. The office made
it. The section between the markers below is rewritten by the office; the rest
belongs to you and your owner.

## Layout

| Path | What goes there |
| --- | --- |
| `GUIDES/` | Procedures your owner stated or confirmed: how to review a PR, how to cut a release. One file per procedure. |
| `RESEARCH/` | Findings that cost real work and will be needed again: a repo quirk, an API behaviour, a verified fact with its source. |
| `PLANS/` | Plans for work that spans sessions. |
| `REPOS/` | The repositories you work in. Work in an existing checkout; clone here only when there is none. |
| `.scratch/` | Disposable working files. Assume it is gone next session. |

## Reading

- Read this file once per session, before other work. Do not re-read it.
- Before a kind of work you have not done this session, look at the guides
  index below and read the one guide that matches. Never read a folder; read
  one file.
- Read `RESEARCH/` and `PLANS/` only when a task or a guide points you at a
  file.

## Writing

- A guide is owner policy. Write one when your owner tells you how to do
  something from now on: the same trigger as remembering. Never invent one.
- A research note is a finding you would otherwise have to re-derive. Not a
  diary, not a transcript, not anything the channel already has.
- A plan is for work that outlives this session. A plan for one turn is a todo
  list, not a file.
- Keep a file under about 4 KB. Names are `ALL_CAPS_WITH_UNDERSCORES.md`, with
  front matter: `title` (quoted), `tags`, `status`, `created`.
- Never overwrite a file you did not create in this task. Append, or create a
  new one.
- Core memory holds pointers, not content. `PR reviews: GUIDES/CODE_REVIEW.md`
  is the whole entry.

<!-- BEGIN QUINTAL MANAGED — rewritten by the office when the fleet starts; add your own notes below the end marker -->
<!-- END QUINTAL MANAGED -->
