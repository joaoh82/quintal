node_modules/
dist/
.next/
out/
build/

# Local database (SQLite/libSQL) — never commit
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Env
.env
.env.*
!.env.example

# Misc
.DS_Store
*.log
coverage/
.vscode/*
!.vscode/extensions.json
.idea/
next-env.d.ts
*.tsbuildinfo

# Maps copied from packages/shared/maps by apps/web/scripts/sync-map.mjs
apps/web/public/assets/maps/

# Fleet configs are per-machine and frequently hold agent keys
quintal.fleet.json
.quintal/

# The build plan: strategy, pricing and competitive analysis. Lives in the repo
# so it can be kept in step with the code, but this repo is public.
plan/
